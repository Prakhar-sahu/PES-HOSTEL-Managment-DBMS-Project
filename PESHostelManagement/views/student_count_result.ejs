<!DOCTYPE html>
<html>
<head>
  <title>PES HOSTEL</title>
  <style>
    body {
      background-color: #f2f2f2;
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 20px;
    }
    h1 {
      color: #007BFF;
    }
    .result-container {
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 5px;
      margin: 20px auto;
      padding: 20px;
      width: 80%;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
    }
    .error {
      color: red;
    }
    .student-count {
      color: #007BFF;
      font-size: 24px;
    }
  </style>
</head>
<body>
  <div class="result-container">
    <h1>Student Count Result</h1>
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
    <% if (studentCount !== null) { %>
      <p class="student-count">Student Count: <%= studentCount %></p>
    <% } %>
  </div>
</body>
</html>
