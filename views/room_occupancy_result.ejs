<!DOCTYPE html>
<html>
<head>
  <title>PES HOSTEL</title>
  <style>
    body {
      background-color: #f2f2f2;
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 20px;
    }
    h1 {
      color: #007BFF;
    }
    .result-container {
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 5px;
      margin: 20px auto;
      padding: 20px;
      width: 80%;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
    }
    .error {
      color: red;
      font-size: 18px;
    }
    .occupancy-status {
      font-size: 24px;
      margin-top: 20px;
    }
    .occupied {
      color: #ff0000;
    }
    .not-occupied {
      color: #00cc00;
    }
  </style>
</head>
<body>
  <div class="result-container">
    <h1>Room Occupancy Result</h1>
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
    <% if (isOccupied === 1) { %>
      <p class="occupancy-status occupied">Room is occupied.</p>
    <% } else if (isOccupied === 0) { %>
      <p class="occupancy-status not-occupied">Room is not occupied.</p>
    <% } %>
  </div>
</body>
</html>
